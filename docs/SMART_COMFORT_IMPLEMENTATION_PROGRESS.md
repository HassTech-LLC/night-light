# Smart Comfort implementation progress

Status: in progress, not built/installed/released. The active completion goal remains open.

## 2026-09-09: first control/recovery increment

- Constitution 1.1.0 adopted and recorded in project memory (T002).
- Save stages settings under the existing thread/kernel locks, persists atomically, then activates Smart. Persistence failure restores the previous in-memory draft and preserves unrelated pending edits.
- Persistent config revisions and optional stale-revision rejection implemented. Cross-writer merge and stable IPC token behavior retained.
- Native Save retries deduplicated within a bounded session cache; mismatched reuse rejected; stale sessions cannot mutate (explicit Off remains available).
- Off during Compare no longer briefly restores the old filter. Off attempts durable storage after immediate neutral intent and reports failure independently.
- Atomic credential-free session receipt added. Unclean/corrupt receipt forces stored automatic state Off; failure to create a durable receipt blocks Smart. Startup and explicit clean quit wired to it.
- Requested RGB, last accepted RGB/time and errors are separate observations. Readback remains explicitly unavailable, not fabricated. Failed writes cannot invoke a success callback or replace accepted output with fictional neutral values.
- Snapshot includes session/sequence/config revision and software observations; recovery/failed-neutral copy is explicit.

Evidence: new regressions first failed for missing behavior; full root `test_*.py` isolated run subsequently passed **170 tests, 1 skipped, 0 failures in 23.48 seconds**. Display backend disabled and config redirected by conftest. `git diff --check` passed. These are software/isolation tests, not installed native or optical verification.

## Still required

## 2026-09-09: Save UI and clock foundations

- Latest-source Save UI now sends session/revision identity, preserves drafts on disk/reconnect errors, queries canonical state after eight seconds without confirmation, offers Check again/Reload saved settings, rejects stale snapshots and flags edits made during a pending Save. Power clears queued slider work.
- Six isolated Node VM scenarios pass in `premium/test-save.cjs`; these are interaction-controller tests, not native end-to-end tests.
- Native command dispatch rejects unknown fields, malformed sessions and unsupported protocol versions. The full versioned reducer and all-entry-point envelope migration remain unfinished.
- Disconnected source pages explicitly identify themselves as a UI preview, disable display controls and avoid fake current-value labels.
- `build_premium.py` succeeds for the latest source. The latest-source test page is served at `http://127.0.0.1:8851/index.html`, distinct from the old concept. Browser identity and nonblank content verified. Screenshot capture worked initially at 1920x911, then subsequent captures timed out after 60 seconds, including a 416x820 check. Narrow screenshot, console-health and rendered Save/native interaction proof are NOT passed. Isolated Playwright/WebView2 fallback permission requested asynchronously; not assumed.
- T006 clock adapters implemented: timezone-aware UTC, independent active and elapsed counters, injected test clocks, cached system-zone refresh, explicit DST gap/fold resolution and bounded restart pause arithmetic. Fifteen clock tests pass, including a read-only native export check. The live clock adapter resolves America/New_York; 598 timezone entries are available. Scheduler integration and all-zone runtime certification remain pending.
- Added tzdata 2026.3 and tzlocal 5.4.4 using wheel SHA256 pins, updated uv.lock without other package upgrades, included license texts and declared frozen timezone data/imports. No new executable installation yet.
- Full isolated root suite: **189 passed, 1 skipped, 0 failures in 24.00 seconds**. `git diff --check` passed.

Clock rationale: [QueryUnbiasedInterruptTime](https://learn.microsoft.com/en-us/windows/win32/api/realtimeapiset/nf-realtimeapiset-queryunbiasedinterrupttime) excludes sleep. The sleep-inclusive adapter uses [GetTickCount64](https://learn.microsoft.com/en-us/windows/win32/api/sysinfoapi/nf-sysinfoapi-gettickcount64), because direct QueryInterruptTime exports were unavailable on this PC. No undocumented fallback silently substitutes wall time. Dependency identity was checked against [tzdata](https://pypi.org/project/tzdata/2026.3/) and [tzlocal](https://pypi.org/project/tzlocal/5.4.4/) release metadata.

## Remaining completion scope

## 2026-09-09: bounded motion and automatic output entry point

- Added pure `smart_transition.py`: versioned candidate limits, inverse-temperature warmth coordinate, independently selectable warmth/dim and normal/catch-up ceilings, endpoint-flat quintic trajectories and constrained braking splices that preserve position/velocity on a new target. Infeasible initial braking states reject instead of clipping/jumping.
- RGB output limiter bounds changes to 1/1024 per 100 ms and rebases gaps of two seconds or more without accumulated output. These are proposed engineering limits, not proof of imperceptibility or physiological benefit.
- Integrated a generation-checked automatic sample entry point into `nightlight_engine.py`. It refuses unknown starting output/Windows suppression, skips unchanged writes and cannot replay samples from before an Off/reset. It does not restart a native easing animation each sample.
- Tests were added red first. Seventeen pure transition tests and seven output tests pass. Full isolated root suite: **209 passed, 1 skipped in 23.11 seconds**; six Node Save UI tests and `git diff --check` pass.
- This is an integration foundation, not the finished controller: the legacy `SmartController.tick` still drives the old schedule. Next replace it through explicit v2 migration with dated solar/personal plans, independent short-night feasible peaks, moving-trajectory catch-up and truthful ETA. Do not check T020/T022/T023 complete until their remaining planner/tracker/integration invariants pass.

## Outstanding full-goal work

## 2026-09-09: dated planner and explicit VPN-independence requirement

- Added `smart_schedule.py`: dated personal and solar plans, explicit polar fallback, per-channel feasible peak bisection, independent warmth/dim ramps, morning neutrality and DST-resolution metadata. Seven targeted schedule tests pass. Final transform-space feasibility and live controller integration are still required before completing T021/T023.
- User explicitly required VPN independence. Added this to the approved plan and setup help. Schedules use saved coordinates and OS timezone, never IP geolocation, proxy endpoints or browser geolocation.
- A regression exposed tzlocal's acceptance of process `TZ` overrides. Replaced the resolver with read-only Windows timezone registry access plus the pinned Windows-to-IANA mapping; honors OS DST-disabled settings without mutating environment variables. Injected test zones remain supported by ClockSource.
- Three VPN tests pass: offline solar/personal planning with blocked sockets/proxy variables, OS zone unaffected by process TZ override, and explicit postal lookup failure with no IP fallback. A VPN can prevent an optional lookup/download, but must not replace saved coordinates. Deliberately changing Windows timezone remains an actual OS preference change, not inferred travel.
- Full isolated suite: **219 passed, 1 skipped in 23.13 seconds**. Six Node Save UI scenarios, latest-source premium UI build and `git diff --check` pass. No new desktop installation or public release.

## Remaining end-to-end work

## 2026-09-09: continuous v2 transform and schedule-output feasibility

- The automatic engine entry point now uses a continuous, neutral-normalized logarithmic RGB mapping. The legacy/manual mapper remains unchanged pending explicit migration. This removes the approximately 6500 K endpoint discontinuity without claiming calibrated temperature or invisible adjustment.
- The v2 mapping has a conservative RGB derivative bound of `3*warmth_rate + .8*dim_rate`, below the final transform limiter for all normal/catch-up and slower policies. Full-trajectory tests cover maximum warmth/dim over an eight-hour night and a reduced-peak 30-minute night, reaching neutral without limiter lag.
- Those integration tests exposed `1-.8` rounding below the strict brightness minimum. Planner arithmetic now preserves the endpoint; quintic roundoff is confined to its mathematical [0,1] range. Strict external-input validation remains intact.
- Focused tests: **47 passed**. First full suite: **234 passed, 1 skipped, 1 failed**: native Tk construction reported a temporarily unreadable `ttk/utils.tcl`. Read-only inspection found the file present, and the same native test passed alone. This unexplained failure is retained, not erased by a retry.
- Full-suite repeat: **235 passed, 1 skipped in 23.61 seconds**. The prior native resource failure remains an unresolved intermittent QA observation.
- Full controller integration, native installation and public release remain unverified; these changes do not complete T022/T023.

## Remaining end-to-end scope

## 2026-09-09: typed owner state and pure intent reducer

- Implemented `smart_state.py` with immutable validated settings, appearance, availability, output observations, snapshots, temporary overrides, native preview tokens and save tickets. Startup defaults Off with availability unknown; a fake/real owner must explicitly establish availability.
- The pure reducer invalidates generations on Off and intent changes; preserves persistent mode through previews; makes duplicate preview tokens idempotent without extending the 10/20-second watchdog; resolves latest intent after expiry; and uses elapsed-duration hold expiry. Stale pause/resume cannot enable Manual/Off.
- Windows exclusion clearing requires explicit Manual resume. Unexpected backend/external conflicts remain latched until Retry, without changing saved mode. Owner-bound Save tickets reject older generations, stale revisions and cross-owner commits. The persistence coordinator must still validate tickets before disk commit and route all entry points through this reducer (T014/T016), so these unit guarantees are not yet app-wide guarantees.
- Validated Smart settings call the dated planner with explicit preferences only; no learning/IP fields are accepted. `NightLightEngine.output_observation` now serializes through the typed observation model, retaining the distinction between write acknowledgement and physical measurement. Readback/ownership implementation remains T013/T044.
- T005 and T012 complete at their model/reducer scope. **34 state tests** and **75 focused state/output/bridge tests** pass. Full isolated suite: **269 passed, 1 skipped in 23.73 seconds**; `git diff --check` passed. No installed app/settings changes or public release. The earlier intermittent Tk resource failure remains documented.

## Remaining full-product scope

## 2026-09-09: native readback, latched conflicts and Retry display

- Added the documented `MagGetFullscreenColorEffect` binding. The engine checks all 25 matrix entries before/after writes, records requested/accepted/readback separately, rejects invalid readback, and does not convert non-diagonal foreign matrices into apparent Kelvin values. Unsupported readback retains explicitly weaker write-ack evidence.
- Unexpected changes and failed readback invalidate pending generations and latch output faults. Further writes, including repeated cleanup neutralization, stop. Explicit Retry only proceeds when the current matrix is neutral or still matches our last owned matrix; it never deliberately overwrites an observed foreign matrix. Off may recover a failed write only after fresh readback proves ownership.
- Integrated readback refresh into the native owner's existing two-second policy poll, fault-aware taskbar/panel status, an allowlisted Retry display command/button and offline help. Retry preserves Off and existing pause/hold intent; Manual restoration requires the explicit action. Save succeeds independently of display availability and says so in both ordinary and timeout-reconciliation UI paths.
- Readback is a software observation, not a physical-screen measurement or an attribution to a specific other app. There is no atomic cross-process ownership lock in this API: another writer can race between check and write, and other display pipelines can be invisible to this readback. These limitations remain for native support/qualification, not a universal conflict-detection claim.
- Verification includes eight isolated Node scenarios, rebuilt current premium assets and the fake native tests in `test_smart_conflicts.py`/`test_premium_ui.py`. T013 is complete at implementation/test scope; T044 lifecycle and T048 hardware proof remain open. No production display API calls, settings mutations, desktop installation or public publication were performed by this test run.

Primary API reference: [Microsoft: MagGetFullscreenColorEffect](https://learn.microsoft.com/en-us/windows/win32/api/magnification/nf-magnification-maggetfullscreencoloreffect).

### Native fixture investigation and final verification for this increment

- A pre-fixture full run reported **282 passed, 1 skipped, 1 failed** loading Tcl `init.tcl`; the native-only subset then reproduced a similar `xpTheme.tcl` failure (**39 passed, 1 skipped, 1 failed**). The affected test passed alone, and installed Tcl files were present with unchanged 2025 timestamps. This was not dismissed as an absent package or fixed by replacing the system Python installation.
- Consolidated the three native UI test modules onto one session-owned Tcl interpreter, using real Toplevels and retaining every construction/layout/interaction assertion. Per-test scaling resets prevent presentation state leaking between tests. No blanket skip, retry wrapper or exception suppression was added. This mirrors the resident app's one-owner lifecycle and follows the [Python Tkinter threading/lifecycle guidance](https://docs.python.org/3/library/tkinter.html#threading-model). It is a test-fixture correction supported by observed ordering failures, not proof of the underlying Tcl defect or fresh-process installation health.
- With the shared owner: native subset **40 passed, 1 skipped**; full run **285 passed, 1 skipped**. Added explicit pause/temporary-hold/recovery-storage-block Retry tests, then final full run **288 passed, 1 skipped in 25.13 seconds**. Focused conflict/output/status/bridge tests **65 passed**; Node scenarios **8 passed**; current premium assets rebuilt; `git diff --check` passed.
- T013 implementation evidence is complete. Native support matrix, fresh-process startup/install qualification and full controller/migration routing remain open. No claim that every other color pipeline can be detected, or that the installed app contains this code.

## Remaining full-product delivery

## 2026-09-09: moving-reference catch-up core

- Added immutable `ChannelTracker` proposals in `smart_transition.py`. Each proposal follows the supplied dated schedule reference (position, velocity and acceleration), rather than a stored deep-night endpoint. A proposed frame does not mutate the accepted state; native integration must commit it only after a valid, generation-matched successful output operation.
- Candidate critically damped feedback uses a 30-second error response, projected onto hard acceleration/rate limits and a feasible stopping envelope. Projection checks both the end state plus future braking distance and any interior direction-reversal extremum. It changes acceleration, not position/velocity by an arbitrary clip. Only float64 boundary roundoff is normalized.
- Boundary tests caught tolerance being incorrectly used to permit slightly weaker braking at a saturated edge. The solver now chooses a strict feasible interval; if floating-point cancellation leaves only the full-braking boundary solution, it selects full braking rather than spending tolerance on weaker braking.
- Ordinary reference changes preserve position/velocity. Callback gaps of two seconds or more explicitly rebase at held output with zero velocity; this discontinuity is not represented as an ordinary acceleration-bounded interval. Regular steps subdivide at at most 100 ms and work at 5/10/20 Hz, without restarting an easing curve on each sample.
- **14 tracking tests** cover convergence at 5/10/20 Hz, moving morning return, reversal, both boundaries, immutable failed proposals, callback gaps, independent warmth/dim/slower policies and seeded target changes. Focused motion/planner/color suite: **53 passed**. Full isolated root suite: **302 passed, 1 skipped in 27.86 seconds**; Node UI **8 scenarios passed**; `git diff --check` passed.
- T020/T022/T023 remain open: catch-up-to-normal handoff, same-model ETA, endpoint completion and native scheduling/migration integration are still required. This is not an installed-app upgrade or a claim of imperceptibility. No user settings, real display or installed binary changed.

## Remaining complete-product scope

## 2026-09-09: coupled handoff and same-model forecast

- Added immutable `TrackingPair` to coordinate independent warmth/dim channels. Catch-up handoff requires both channels to remain within the 1/1024 error window for two active seconds; an interval margin accounts for motion between samples. Position, velocity, acceleration and stopping viability must fit the normal policies before switching. Handoff changes the policy, not the current position or velocity.
- Wake/callback discontinuities discard accumulated matching time and rebase without output leap. Rejoin preserves state while allowing the disclosed catch-up ceiling. Both slower multipliers retain their independent normal warmth/dim policies.
- Added bounded, cancellable `forecast_handoff`, which simulates the same moving-reference tracker. It returns an estimate, already-following state, beyond-horizon uncertainty, unavailable-reference result or cancellation. It does not mutate the accepted tracker. This ETA is specifically catch-up-to-schedule completion, not a physical-screen or final morning-neutral ETA.
- Optimized safe tracker frames to avoid unnecessary braking-interval bisection when the requested acceleration already satisfies both envelopes; all prior boundary/reversal invariants remain covered.
- Twelve handoff/forecast cases pass, including real-model comparison at 5/10/20 Hz, moving morning reference, two-channel matching, callback gaps, cancellation and slower-policy handoff. Focused motion/planner/color suite **65 passed**. Full isolated suite **314 passed, 1 skipped in 25.01 seconds**; Node UI **8 scenarios passed**.
- One local pure-model benchmark for the full-span slower catch-up estimated **3,675 simulated seconds** in **0.699 seconds of computation**. This is not a native app performance claim. Forecast work must run off the UI thread with generation-aware cancellation/cache invalidation; that integration is still required.
- Default pytest discovery now excludes historical `audit/` sandboxes while preserving them on disk. Compared node IDs: explicit current-root suite **315**, default suite **315**, zero difference. No current assertions were dropped and no archived app was run.
- T020/T022/T023 remain open for remaining endpoint completion, native forecast/scheduler integration and full-controller invariants. Installed app, user settings, display output and public site are unchanged.

## 2026-09-09: native Smart runtime adapter and VPN-invariant schedule integration

- Added `smart_runtime.NativeSmartRuntime`, an opt-in owner adapter connecting the typed reducer, explicit dated planner and coupled tracker to `NightLightEngine.apply_automatic_sample`. Automatic ticks do not restart the legacy 25-second animation. Proposed state commits only after output acceptance, matching owner/native generations and exact accepted RGB alignment.
- Off invalidates runtime motion before requesting neutral and does not require a working clock. Sleep-inclusive pause expiry, callback gaps, wall-clock jumps/rollback and timezone changes rebase at the last confirmed output, without spending missed time on a catch-up jump. Native faults do not commit fictitious trajectory progress.
- Explicit v2 color-policy selection enables consistent Manual-to-Smart endpoints without changing the display on selection. Unknown/foreign and legacy intermediate RGB values are not inverted into fabricated Kelvin coordinates. Windows suppression clearing does not silently restore v2 Manual; explicit Retry is required. Legacy policy remains the default outside this adapter.
- Runtime solar tests recompute the Dearborn Heights schedule with changed HTTP/HTTPS proxy and `TZ` environment settings and DNS resolution forbidden. Results remain identical using saved coordinates and the injected Windows-zone equivalent. This is deterministic adapter testing, not a claim that a real VPN client was connected or that software changing the actual Windows timezone should be ignored.
- Twenty-one runtime tests include bounded native writes, Off/write races, failed writes, native epoch cancellation, foreign matrices, five discontinuity types, temporary holds, Windows recovery, non-mutating policy selection, VPN independence and all four appearance extrema. The extrema tests caught floating-point subtraction producing brightness just below 0.2; the native appearance conversion now retains the valid endpoint.
- Final source regression: **335 passed, 1 skipped in 25.07 seconds**; isolated Node Save UI **8 scenarios passed**; `git diff --check` passed. Fake Magnification backend and temporary test configuration only; no real display, installed binary, user settings or public website changes.
- This adapter is **not yet selected by the live legacy `SmartController`/tray/bridge**. Versioned migration, atomic Save coordination, all-command routing, preview reconciliation, forecast workers and final endpoint policy remain required before activating it for users. No broader task checkbox is closed by these adapter tests alone.

## 2026-09-09: guarded v2 persistence and continuous preference replanning

- Added opt-in `SmartStore` to validate/stage a versioned Smart draft, persist canonical preferences and a credential-free Save receipt, then commit the native owner. It requires explicit `schema_version=2`/`migration_state=v2`; it does not choose migration on the user's behalf. Compatibility flags remain neutral; the v2 owner has the sole new-mode authority.
- Added a final owner-ticket guard after temporary-file fsync and immediately before atomic replacement. A newer Off during staging rejects the older Save and preserves the old file. Disk failures retain prior runtime/configuration and pending unrelated edits. Owner-thread checks reject cross-thread commit calls.
- Bounded 128-entry request acknowledgement cache plus the last durable Save receipt allow acknowledgement replay without replaying activation, including after Store reconstruction and a subsequent Off. Reusing a request identity for a different validated draft fails. Returned settings are generated from allowlisted validated preferences, never arbitrary config/receipt fields or IPC credentials. This is last-save restart reconciliation, not an unbounded persistent request history.
- `SmartStore.off` requests native Off first, then attempts persistence independent of stale expected revisions. Storage failure has a distinct `off_not_saved` result. `ConfigManager.begin_session` now neutralizes `intent_v2` and clears `override_v2` as well as legacy flags on unclean recovery; chosen Manual appearance, Smart settings and unrelated preferences survive. A failed recovery journal also forces v2 Off in memory and blocks automation.
- Native acceptance of saved settings preserves moving position/velocity. When changing the slower multiplier, it brakes using the prior feasible limits before adopting the new limits at zero velocity. Failed samples do not spend braking time or commit proposed motion. A pending reference rejoin is consumed only after accepted output; normal handoff matching is not reset every tick.
- Fifteen Store tests cover durable-first commit, disk/stale-writer failures, newer Off, replay after Off/reconstruction, reused identities, explicit migration gate, owner-thread enforcement, saved-but-display-blocked/clock-unavailable results, continuous replanning/braking, failed braking frames, neutral handoff and legacy out-of-range output uncertainty. Recovery/transaction regressions first reproduced the missing guard and stale v2 mode, then passed after implementation.
- Final source regression: **353 passed, 1 skipped in 25.36 seconds**; Node Save UI **8 scenarios passed**; `git diff --check` passed. All native calls used the fake backend and all config writes used temporary test directories. No installed binary, user preferences, physical display or website changes.
- T008/T014/T015/T016/T023/T045 remain open at full app scope. Store is currently a synchronous owner-thread coordinator, not the eventual nonblocking disk-stage/owner-finalize pipeline; lock/slow-disk responsiveness still requires that integration and testing. Migration/backup/rollback UI, live tray/bridge routing, persistent override restoration, full protocol reconciliation and native Save/Off proof are still required before activating v2 in an install candidate.

## 2026-09-09: nonblocking persistence worker and shutdown reconciliation

- Added `AsyncSmartStore`: a single serial disk worker, bounded pending requests, immutable staged drafts, invalidatable Save leases and owner-thread `poll()` completion. The worker never calls the display engine or Tk. A duplicate pending request produces one write/one owner completion; changed payload reuse is rejected.
- Off cancels pending Save leases and requests neutral immediately on the owner, before queueing its disk update. Cancellation is checked before work and again after staging immediately before replacement. If an older Save already won the filesystem race, the owner-generation check still prevents its late completion from enabling Smart. The queued Off persists after that write.
- Added `ConfigManager.transactional_update_isolated`: staged worker candidates are not assigned to shared `config.data` before durable replacement. GUI readers therefore cannot see an uncommitted draft while fsync is held. Existing legacy synchronous behavior is preserved for its existing callers pending v2 routing.
- Added the runtime's immediate invalidation subscription and a volatile final-intent confirmation gate. `finish_session` refuses to issue a clean receipt while disk work is pending or durable intent/settings disagree with the current owner. A failed Off save remains recoverable-neutral on next startup; successful Retry saving reconciles the gate. This covers the Save-committed/Off-requested/Off-disk-failed case rather than closing a receipt over stale Smart intent.
- Seven event-controlled worker tests prove slow-fsync Off responsiveness, private staging, native commits only on owner polling, late Save after Off, duplicate pending requests, failed-save preservation, and failed-Off/Retry shutdown safety. Test barriers wait for an actual worker queue operation; no timed sleeps or real user data/display calls were used.
- Final source regression: **360 passed, 1 skipped in 25.55 seconds**; Node Save UI **8 scenarios passed**; `git diff --check` passed. The synchronous coordinator tests continue to pass after sharing preparation/completion logic.
- The asynchronous store still needs to be selected by the v2 desktop command router and pumped by the existing owner poll. Migration, setup/bridge responses, all entry-point routing, shutdown drain UX and native latency/failure qualification remain open. Worker behavior is tested in isolation; it is not yet a claim that the installed UI uses this path. Installed app, real settings, physical output and public site are unchanged.

## 2026-09-09: preview-gated migration, private backups and fresh-install identity

- Added three synthetic/redacted legacy fixtures (learning/Smart, Off/custom colors, older out-of-range appearance) and twenty migration/startup cases. `inspect_legacy` returns only a UI allowlist, not IPC credentials or routine records. Proposals bind explicit new settings to the full original configuration fingerprint/revision. Keep returns no changes; Switch requires native preview approval. The future bridge must cache native proposals/approval rather than trust a `previewed` Boolean supplied by web content.
- Switching preserves saved mode (including Off), valid Manual appearance, location/explicit legacy times, theme/startup choices, unknown fields and history. Legacy raw preferences are retained privately; the active IPC credential is not duplicated into that namespace. Unsupported old appearance bounds require explicit replacement rather than clamping. Valid unexpired pauses/holds preserve their original expiry; ambiguous timing requires review or an explicit clear choice. No inferred routine values become v2 timing authority.
- Added a hard v2 guard to legacy scheduling and routine collection, including direct record calls even if an old collection flag remains true. `legacy_pending` retains its already-consented legacy behavior; v2 stops new collection without silently deleting history.
- `commit_migration` runs a locked worker-side transaction: validate current disk/local state, preserve exact original configuration bytes in a deterministic private backup, validate/reuse that backup, persist a credential-free migration receipt, then atomically change schema. Failed/cancelled final writes leave legacy configuration unchanged and retain the verified backup for retry. A corrupt or mismatching existing backup stops migration instead of being overwritten.
- Added `private_config_files` using documented Windows owner/group/DACL APIs. Access is assigned and compared while temporary files are still empty, before writing credential-bearing bytes. Existing configuration replacements use the same access-preservation check. Six tests use actual Windows file-security APIs only on temporary fixture files, including a protected DACL; no real user configuration ACLs were changed. The comparison normalizes only `SE_DACL_AUTO_INHERITED` metadata added by Windows, retaining protection and every ACE/owner/group. Auditing SACLs and broader encrypted/enterprise-profile qualification are not covered by this helper.
- Fresh-install detection now records actual creation and retains the initial writer lock through creation. Existing Off files are not considered new. Simultaneous constructors identify exactly one creator. The older first-launch race fixture was moved from its removed save callback to the initial acquisition boundary; its temperature/credential preservation assertions remain, with an added not-fresh assertion.
- Final diagnostic full run: **386 passed, 1 skipped in 25.65 seconds**; focused worker/access/migration run **33 passed in 0.37 seconds**; Node Save UI **8 scenarios passed**; `git diff --check` passed. One preceding full run hit a five-second worker-barrier timeout and finished in 68.50 seconds. It did not reproduce in the focused or full thread-stack diagnostic runs. No timeout increase/retry wrapper was added; root cause remains unproven and latency qualification stays open.
- T009 is complete at fixture/expected-case scope. T045/T056/T067 remain open: migration UI, native proposal/consent ownership, startup/controller activation and complete binary/config rollback are not yet wired/verified. The receipt explicitly says `binary_retained=false`: it records source binary bytes' hash, not retained executable rollback, signature verification or install proof. These operations were exercised with temporary fake binary files, never the installed executable. Installed app, real settings, display output and public site remain unchanged.

API references: [GetNamedSecurityInfoW](https://learn.microsoft.com/en-us/windows/win32/api/aclapi/nf-aclapi-getnamedsecurityinfow), [SetNamedSecurityInfoW](https://learn.microsoft.com/en-us/windows/win32/api/aclapi/nf-aclapi-setnamedsecurityinfow), and [security descriptor control flags](https://learn.microsoft.com/en-us/windows/win32/secauthz/security-descriptor-control). Access-copy proof here is limited to the checked owner/group/DACL scope; it is not a blanket Windows security qualification.

## 2026-09-09: restart restoration and persistent resident commands

- `NativeSmartRuntime.from_config` restores only explicit v2 data after recovery storage is opened, establishes neutral ownership again, rejects malformed policy/intent, and restores only the remaining pause/hold duration. Fifteen restart tests cover neutral-first Smart, Manual suppression/retry, invalid configuration, unclean exit, foreign transform ownership, clock rollback and Save/restart round trips. Legacy emergency Off now requests native neutral before configuration writes.
- Added `AsyncSmartStore.submit_command` for Manual, Smart, temporary adjustment, pause and resume. Commands apply on the resident owner thread; immutable persistence patches run on its existing serial worker. Acknowledgements distinguish pending, saved, superseded and unsaved states. Owner polling never replays the native action. Duplicate command requests within the bounded resident cache do not extend pauses or re-enable output after Off.
- Durable overrides include original duration and UTC start/expiry/last-seen fields. Clean-exit reconciliation now compares the actual override, not merely Smart mode/settings; a failed temporary-adjustment write cannot be falsely marked clean. Timestamp comparison permits only serialization-level rounding. Command writes include the policy version required by the strict restart loader.
- Six command-store tests exercise Manual durability, pause/resume expiry, failed-adjustment reconciliation, duplicate requests, held worker/Off ordering, and clean restart of a temporary adjustment. New tests first failed for the missing command API; the restart integration also caught and corrected the missing policy-version field.
- Final full Python regression: **407 passed, 1 skipped in 26.08 seconds**. Focused command/async/restart suite: **28 passed in 0.58 seconds**. `git diff --check` passed. The earlier session 81053 no longer had a live process handle, so its missing completion output is not used as evidence; the fresh full run above is authoritative. The previously observed intermittent worker timeout remains unexplained and is not closed by a passing rerun.
- These are source-level owner/store integrations, not completed tray/IPC/WebView routing. Desktop factory selection, new migration/setup UI, request session validation across process restarts, shutdown drain UX, preview restore/watchdog, forecast presentation, native visual checks, installer and website remain open. No installed executable, real user settings, display state or public site was changed.

## 2026-09-10: explicit v2 desktop routing and worker-hang diagnosis

- Added the `SmartDesktopController` facade and selected it in real `TrayApp` startup only when configuration already contains the explicit v2 migration marker. The v2 branch skips legacy output restoration and does not instantiate the legacy scheduler/learner. Existing installations are not silently migrated.
- Routed tray toggle, presets, taskbar strength, pause/resume, emergency Off, embedded power/mode/adjust/pause/resume, Compare and Retry through that same owner. Windows Night Light clearance bypasses the old direct-engine rejoin animation. The v2 owner timer runs every 100 ms, with shell/UI work throttled to one second and no idle-history sampling. Worker shutdown is nonblocking; pending durability still prevents a clean recovery receipt.
- Embedded snapshots now expose v2 settings and command persistence outcomes, not a fabricated legacy timeline. Legacy Save/learning/location-deletion forms explicitly refuse v2 mutations until their replacement UI is ready. This is an acknowledged unfinished UI dependency, not a completed setup experience. No installer or real user migration was run.
- Six integration tests call the actual tray/embedded handlers with a temporary config and fake display backend: owner-routed presets, embedded pause/Off, no-learning/100-ms ticking, allowlisted snapshot/outcome, emergency Off during Compare, and Windows-policy clear without legacy rejoin. The isolated AST legacy-toggle test imports the new routing predicate without weakening its restart assertions.
- Diagnostic full run reproduced the previously intermittent worker timeout and identified the exact dependency: the settings worker was inside `tkinter.font.Font.__del__`, triggered by cyclic collection during `json.dump` in `ConfigManager._save_locked`, while the test main thread waited on the worker barrier. Added owner-thread `gc.collect()` after Tk fixture teardown so destroyed font/widget cycles are finalized on their Tcl thread. No timeout increase, assertion suppression or global collection disabling was used. This explains the observed test-suite hang; real packaged GUI/thread responsiveness remains a separate qualification.
- Two fresh post-fix full suites: **413 passed, 1 skipped in 26.41 seconds**, then **413 passed, 1 skipped in 26.36 seconds**, both exit code 0. Node Save UI **8 scenarios passed**; `git diff --check` passed. Two hung diagnostic pytest sessions were interrupted after capturing their failures; no installed app process was stopped.
- Remaining immediate integration work: new v2 settings/onboarding/migration controls and native preview approval, same-model timeline/ETA, request-result UI reconciliation, clock-independent preview watchdog/restore bounds, shutdown drain feedback, packaging/installer/site and real desktop qualification. Broad plan checkboxes stay open. Installed binary, live config, physical display and public website remain unchanged.

## 2026-09-10: Smart Comfort settings form and asynchronous save feedback

- Added explicit personal/solar timing, evening warmth/dimming targets, independent lead times, gradual/extra-gradual pace and sunset offset controls. Existing coordinates and fallback-time inputs feed the v2 settings model; hydration preserves explicit saved values. Legacy profile/bedtime/learning controls are hidden for v2, with no silent migration or inferred timing. Added concise explanations distinguishing evening targets from current output and avoiding imperceptibility guarantees.
- Added the allowlisted `save_comfort` bridge action, validated native `SmartSettings` staging and retained asynchronous Save outcome in desktop snapshots. The form sends its draft revision and request ID, keeps the button pending after transport acknowledgement, and finishes only on matching native saved/failed/superseded outcomes. An absent v2 timeline is hidden rather than showing invented legacy times. Migration preview/approval and first-install activation are still separate unfinished work.
- Validation: new native pending-to-durable bridge integration test passed; **414 Python tests passed, 1 skipped in 26.37 seconds**. JavaScript Save controller **9 isolated scenarios passed**, including a v2 transport reply that must not clear pending state before native confirmation. UI assets/WebView2 host rebuild and `git diff --check` passed. This is not a rebuilt or installed NightLight executable.
- Browser-harness available; source UI `http://127.0.0.1:8851/index.html` loaded at 1920x911, then a clearly labelled simulated bridge fixture at 416x820. Initial screenshots succeeded. Coordinate click opened Schedule & preferences. Subsequent DOM inspection confirmed expanded settings, visible v2 controls, personal timing, no captured JavaScript errors and no horizontal overflow. `Page.captureScreenshot` then timed out after 60 seconds, leaving expanded-form visual fidelity and full rendered save interaction unverified. No Playwright fallback was used and no physical display output was invoked.
- Remaining: native migration/onboarding UI and consent ownership, save/restart reconciliation refinements, previews/watchdog/restore bounds, dated timeline/ETA, full material/accessibility/visual checks, installer/site/direct download and installed/runtime/release evidence. No live settings, installed executable or public website changed.

## 2026-09-10: independent preview watchdog and comfort preview controls

- Added `ClockSource.elapsed_now` as a separately validated sleep-inclusive deadline counter, independent of UTC, active-time and Windows-zone sampling. If civil sampling fails, the native owner still expires Compare/comfort previews and temporary overrides. If the deadline counter itself fails, previews end conservatively. Off remains independent of all clock adapters.
- Ending a preview restores the latest effective intent, not a captured pre-preview state. Smart restoration requests a 500 ms transition to its current dated reference; Manual and temporary appearance use current owner values, and Off requests neutral. Missing schedule/clock uses neutral rather than stale solar timing. Conflict/unavailable state prevents competing writes. Save during Compare restores the newly committed settings. These tests verify command routing and requested duration, not physical completion latency.
- Added a native-backed 20-second comfort preview and Restore button in the v2 settings form. Preview does not enable the saved mode. Snapshots distinguish comfort from original-color Compare and expose native remaining seconds; the countdown is a non-live timer rather than a per-second announcement. Power/Off cancels preview even when the underlying saved mode is Off.
- Seven watchdog tests first reproduced missing timeout, failed release and absent bounded restore. Coverage now includes civil-clock failure, sleep expiry, newer Off winning, newly saved preference restore, neutral fallback and foreign-transform preservation. An additional desktop-handler test verifies comfort preview/remaining time, unchanged saved Off and power cancellation.
- Final full source suite: **422 passed, 1 skipped in 27.98 seconds**. Focused watchdog/runtime/desktop suite: **36 passed in 0.42 seconds**. Existing JavaScript Save controller **9 scenarios passed**; UI asset/host rebuild and `git diff --check` passed. The earlier source-browser screenshot timeout remains an open rendered/native QA limitation; these changes have not been claimed visually or physically verified in the installed app.
- T031 and broader preview/onboarding gates remain open for packaged-host close/blur/accessibility, true latency measurement and migration-native approval integration. Migration, first-run setup activation, complete UI polish, installer/site/direct download and installed/release evidence remain unfinished. Real configuration, installed executable, physical display and public site are unchanged.

## 2026-09-10: fresh-install activation and quick-start guidance

- Real tray startup now calls `initialize_first_install` before opening recovery storage. Only a configuration this process actually created from untouched defaults is eligible. Existing Off installations, imported/modified configurations and concurrent writers are not treated as new users. The write uses expected disk revision; another process's settings are preserved. It initializes v2 Off, no routine collection, no automatic startup and incomplete first-run guidance. No installed/live config was run through this initializer.
- Added an inline first-run guide: choose personal/solar timing, understand temporary 20-second preview, and wait for durable Save confirmation. Set up Smart opens and focuses the settings form; Skip is explicitly session-only and sends no native command. Quick start reopens without being dismissed by periodic state updates. The Skip label reflects whether current mode is neutral. Completing a real durable Smart save marks the onboarding record complete in the same transaction.
- Added taskbar pinning and separate tray-icon instructions, explicit-target versus measured-bedtime wording, and VPN/location guidance. Pinning/startup remain user choices; there is no simulated Pin button or automatic registry action.
- Four first-run tests cover fresh neutral setup, existing Off preservation, concurrent-writer protection and modified-config rejection. Desktop save assertions verify onboarding completion; the tenth JavaScript scenario checks Skip sends no command and reopened guidance survives snapshots. Full source suite **426 passed, 1 skipped in 27.17 seconds**; focused first-run/desktop suite **12 passed in 0.40 seconds**; JavaScript **10 scenarios passed**; UI assets/host rebuilt; `git diff --check` passed.
- Rendered first-run layout/keyboard/physical-neutral proof is still pending, alongside the previously observed browser screenshot limitation. Existing-user migration preview/approval/rollback UI, dated timeline/ETA, final UI/material/accessibility checks, installer/site/direct-download wiring, actual installed verification and release gates remain unfinished. No installed binary, real user settings, physical display or public site changed.

## 2026-09-10: output-planner-backed desktop timeline

- Added `NativeSmartRuntime.scheduled_plan` to expose the current or upcoming dated plan from the same cache/model used by automatic output. Daylight selects the upcoming night rather than yesterday's expired evening; no extra network/location inference is introduced.
- Desktop snapshots now carry scheduled start, evening-ready and neutral times, exact UTC/date counterparts, each channel's start, Windows-zone identity, DST resolution, occurrence identity and reduced-peak status. Local labels include weekday when the event is on another day. Solar, personal and explicit polar fallback are named distinctly. A clock/planner failure returns unavailable rather than stale cached timestamps.
- Embedded timeline labels now say Starts / Evening ready / Neutral for v2. The caption explicitly separates scheduled times from catch-up ETA and notes that marker spacing is not to scale. Short-night peak reduction is explained. This connects real planned timing to the panel but does not complete the outstanding same-model catch-up forecast worker or physical output claims.
- Six new timeline tests cover planner equality/dates, upcoming-night selection, no stale timeline after clock failure, real snapshot integration, first DST fold occurrence in America/Detroit and labelled high-latitude fallback. Full source suite: **432 passed, 1 skipped in 26.39 seconds**; focused timeline tests **6 passed in 0.19 seconds**; JavaScript Save/onboarding **10 scenarios passed**; assets/host rebuild and diff whitespace check passed.
- Rendered timeline QA remains open with the prior browser screenshot limitation. Existing-user migration approval/rollback UI, forecast ETA integration, final design/accessibility and packaged behavior, installer/site/direct download, installed verification and release evidence remain unfinished. No live user settings, installed app, physical display or public website changed.

## 2026-09-10: existing-user read-only migration review

- Connected `preview_migration` to the private desktop bridge. It validates the expected config revision and explicit Smart settings, creates an immutable native `MigrationProposal`, and returns an allowlisted dated schedule review with current mode, preservation information and human-readable local date/zone labels. No native display calls or configuration writes occur. The full source fingerprint/credential remains native; client `previewed` fields are rejected. A failed new review invalidates the preceding proposal; snapshots suppress reviews after revision changes.
- Existing-user controls now expose a separate review workflow using the new timing fields, with a clearly labelled Preview proposed schedule action and Keep current policy. Enter submits a read-only review rather than legacy Save. Keep restores the previous unsaved legacy form draft. Local UI updates do not advance the native snapshot sequence. The screen explicitly says switching remains unavailable until rollback/activation integration is ready; it does not claim a schedule review physically previewed the screen.
- Three native tests cover read-only review, credential/fingerprint exclusion, forged approval rejection and stale revision rejection. JavaScript scenario eleven verifies no mutation on opening review, review-only form submission, prior draft restoration and acceptance of the next real snapshot. Full Python suite **435 passed, 1 skipped in 26.55 seconds**; final review/bridge focused check **41 passed in 0.48 seconds**; JavaScript **11 scenarios passed**. UI assets/host rebuilt and `git diff --check` passed.
- This completes the read-only review connection, not migration approval/commit/rollback or installation. Native generation-safe switching, retained binary/config rollback, rendered migration comprehension/keyboard checks, forecast ETA, installer/site/direct download and installed/release proof remain unfinished. Existing live configuration, installed binary, display and public website were not changed.

## 2026-09-10: retained migration binary/config pair

- Migration now streams the source application into a same-directory private, content-addressed `.payload` backup before committing v2 configuration. It does not register another app, create a shortcut or execute the retained file. Owner/group/DACL are copied from the private configuration before bytes are written; streaming and reread SHA-256 verification, bounded size and no-overwrite publication are enforced. A source changed after initial hashing fails instead of publishing mismatched bytes. Existing retained files are reused only after hash/access checks.
- Version-2 migration receipts now record `binary_retained=true` and the exact binary backup name alongside the preserved configuration/hash. The new receipt filename includes both content identities and does not overwrite older version-1 receipts. A failed commit may leave valid private backups for verified reuse, but does not overwrite the active source configuration.
- Added read-only `verify_retained_migration_files` for the future rollback action. It rejects missing/linked/path-traversing or wrongly named files, access mismatches, oversized/corrupted payloads and inconsistent source revision. It parses the same verified config bytes rather than rereading a mutable file. Its result explicitly leaves signature and activation verification false; retained bytes alone are not a qualified executable rollback.
- Tests first failed for missing retention/verification and the old false retention receipt. Eight additional cases cover streaming/reuse, source mutation, corrupt backup preservation, ACL failure before publication, valid pair verification and binary/config/path tampering. Full source suite **443 passed, 1 skipped in 26.72 seconds**; final retention/migration/access subset **34 passed in 0.38 seconds**; Node UI controller **11 scenarios passed**; diff whitespace check passed.
- All binaries/configurations in these checks were synthetic temporary fixtures, never executed. Existing-user switch orchestration, actual rollback activation/signature compatibility, installed proof, installer/site download integration and remaining full-plan gates are still unfinished. The installed app, actual user configuration, real display and public site are unchanged.

## Remaining full objective

### 2026-09-10: first compiled private setup candidate

- Downloaded the official NSIS 3.12 ZIP into `audit/installer-toolchain`, extracted it locally and verified `makensis /VERSION`. Recorded archive SHA-256 `56581f90db321581c5381193d796fffcf2d24b2f8fed2160a6c6a3baa67f2c4f`, compiler hash and all 441 extracted files' aggregate digest in `installer/toolchain.json`. The compiler builder refuses a changed tool tree. This is local development tooling, not an app install or public signing identity.
- Added `build_installer.py`: verifies the release archive and current-source manifest, rejects unsafe/duplicate Windows member names, stages bounded payloads, includes NSIS notices, compiles from the pinned toolchain and writes setup provenance. Existing output directories are never overwritten. Warnings fail compilation (`/WX`).
- Added a real NSIS script: per-user/zlib, Windows 11/x64 and WebView2 registry checks, explicit official Microsoft download-page choice if runtime is absent, no app launch/filter/startup enablement. It refuses an existing installation root, known Start shortcut or uninstall record. It does not implement upgrade/rollback/partial-copy cleanup/removal yet; uninstallation explicitly refuses deletion. **Do not execute this private candidate in a real user account.** Native setup behavior is not yet qualified.
- First compile exposed an uninstall-string quoting warning; corrected it and rebuilt from current source. Final isolated candidate `20260910e` full app build, verified release ZIP and setup compile all succeeded with warnings-as-errors. ZIP SHA-256 `f92941b6c71db18fb056cf5b58a3b294ba95314769ffd415cbf7d6e892dfc8d2`; `setup/NightLightSetup-private.exe` SHA-256 `fcfbaf70d2782026bb1501e94480ed0bb60bab40fbbfa4543633a7ba14be973e`. `SETUP-BUILD.json` binds compiler/script/source ZIP/output. Candidate d and its syntax-only fix are superseded diagnostics, not current artifacts.
- Twelve new installer contract tests cover destination mapping, traversal/shell/Windows aliases/duplicate names, changed compiler tree and explicit no-launch/no-delete script boundaries. Full suite **504 passed, 1 skipped in 27.29s**; final post-quoting-fix installer/exit/binding suite **28 passed in 0.23s**. These are source/compiler checks, not installer execution proof.
- No setup or app executable was launched, installed, published, or used to change the real user's app/settings/shortcuts/display. Remaining immediate installer work: exact process/file exit proof, owned payload/shortcut receipts, staging/swap/recovery, compatible rollback and safe removal, then disposable-account/VM validation. Signing/site/live download and all broader product gates remain unfinished.

### 2026-09-10: installer-safe authenticated exit command

- Added client-only `--request-exit`. It cannot fall through to owner creation, rejects combined flags, and returns 0 only for authenticated request acknowledgement (2 for unconfirmed/invalid requests). The resident sends acknowledgement before queueing its normal GUI-owned quit/neutral cleanup. This is not process-exit or unlocked-file proof; installer must wait for exact owned processes and files before swapping payloads.
- Made IPC's normal token creation lazy and extracted pure configuration path resolution to `config_paths.py`. The exit client reads bounded existing config bytes without initializing ConfigManager, creating directories/settings/tokens, repairing malformed files or sending reusable credentials over the socket. Existing normal app startup uses the same directory resolution.
- Added nine cases covering a fresh subprocess with missing config and no display/config imports or writes; existing credential read without rewriting; malformed/short/nonobject data preservation; exclusive CLI flags/no resident fallback; and real ephemeral mutually authenticated QUIT dispatch with simulated GUI callbacks. Focused suite **49 passed in 4.22s**; full Python 3.14.7 **492 passed, 1 skipped in 27.19s**. Diff whitespace check passed.
- Added `installer/README.md` with exit-status semantics and remaining setup requirements. No request was sent to the user's running application. No app/config/display/shortcut/site mutation occurred. Candidate c predates this source change; it is not proof of the new command. NSIS script/compiler, process/file exit verification, stable target, rollback, signing, full install and website download remain unfinished.

### 2026-09-10: embedded running-build identity and About

- Added `build_identity.py` and generated `assets/BUILD-IDENTITY.json` inside the packaged app, containing version, source inventory digest/build ID, source revision/dirty state, runtime and explicit private-candidate channel. Source development runs never borrow generated candidate receipts. Missing/malformed/wrong-product/unqualified-channel receipts show identity unavailable, never a guessed installed version. These records are not Authenticode verification or installer proof.
- Premium About now displays the identity of the running package rather than website metadata. The bridge caches the embedded identity and exposes only allowlisted fields. Packaging verifies identity metadata against recorded source, runtime and application SBOM version. Generated receipt is ignored by git.
- Full Python 3.14.7 source suite **483 passed, 1 skipped in 27.42s**; JavaScript **13 scenarios passed**. Built current-source isolated candidate `audit/smart-comfort-candidate-20260910c`; read the receipt directly from the PyInstaller archive without executing it and confirmed exact match with BINARY-ORIGINS. Embedded build ID `b8edc37078595705`, version `0.1.0`, CPython `3.14.7`. The build ID is a source-inventory identifier, not the executable's SHA-256.
- Private unsigned ZIP passed archive/build-binding checks; SHA-256 `d85c3a6e51e0c833b2b7fa5f3de47e594dc11f824fc99a57de37f5a585225678`. Candidate c supersedes b for source/packaging diagnostics only. Neither was installed, launched or published. Native rendered About is not yet verified.
- Installer discovery found no local NSIS compiler or existing installer files. Official [NSIS download](https://nsis.sourceforge.io/Download) currently lists 3.12. Re-read [Microsoft WebView2 distribution](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/distribution): runtime detection must not be inferred from Edge browser presence; its documented HKLM/HKCU checks and explicit missing-runtime handling remain installer work. No compiler was downloaded or installed in this turn.
- Remaining distribution work is still substantive: pinned compiler and setup script, single stable installation target, previous-payload identity/rollback execution, signing, native installation verification, website/download integration and public authority gates. This increment closes the app's identity-display gap, not the installer or full release.

### 2026-09-10: desktop migration interlocks and owner adoption

- Connected the native migration coordinator to `TrayApp.begin_smart_migration`. It rejects an active/completed migration, unavailable recovery storage and an open legacy editor; native caller must supply the installer-verified previous binary. There is still no browser action accepting a path or fabricated proposal. Public switching remains disabled until installer identity and rollback activation are qualified.
- Desktop scheduler timer now retains its handle so starting a handoff cancels the older 25-second wakeup and polls at 100ms. Pending handoff prevents legacy scheduling/learning; successful polling adopts `SmartDesktopController` on Tk's owner thread and retains hotkey status. A real private transaction test uses the actual desktop method and v2 controller with synthetic never-executed backup bytes, verifying saved Off remains Off.
- Tray toggle, emergency Off and zero-strength commands request neutral immediately during handoff. Conflicting taskbar controls, nonzero presets/strength and premium mutations are interlocked. Windows-policy refresh cannot invoke legacy rejoin while switching; closing a preview cannot restore a stale captured appearance. Cleanup delegates cancellation/Off persistence to the worker without waiting on migration storage. Off persistence or adoption failures keep the interlock and recovery receipt open, including failures before the configuration became v2.
- Premium snapshots explicitly describe pending/failed handoff, omit stale timeline markers and keep Off available. Other display and Save controls are disabled during it. The JavaScript fixture now actually exercises grouped ID/mode selectors instead of returning an empty list; this caught and fixed accidental Save re-enabling while a request remained pending.
- Focused desktop/bridge/coordinator suite **68 passed**; full Python 3.14.7 suite **474 passed, 1 skipped in 27.51s**; JavaScript **12 isolated scenarios passed**. No installed app, live display or user configuration was changed. Rendered/native latency, installer-provided previous-version identity, rollback execution, visible migration consent, fresh full build and actual installation/site publication remain unproven/incomplete.

### 2026-09-10: asynchronous migration handoff foundation

- Added `MigrationSwitch`, a serial-worker/GUI-poll coordinator around the existing private migration transaction. The worker only writes storage; adoption runs only when the GUI polls a completed job. Its callback is the future desktop owner-adoption boundary, not a worker-thread display call.
- Off requests neutral immediately without waiting for a held storage job, cancels the migration commit guard, and queues a durable Off after any racing migration commit. The Off patch is computed after that commit, so a newly written v2 Smart intent is also cleared. Adoption waits for durable Off; failure keeps recovery open and requests neutral without exporting private exception details.
- Seven tests cover GUI-only adoption, Off before commit, Off after commit/before poll, Off-write failure, adoption failure, preservation of recovery flags, and the real private transaction using synthetic never-executed binary bytes. Retained binary/config verification and unrelated theme preservation passed. Focused migration/retention suite: **35 passed in 0.40s**.
- Full Python 3.14.7 source suite: **459 passed, 1 skipped in 26.98s**. Diff whitespace check passed.
- This is a tested coordinator, not the user-visible migration switch. Legacy desktop command/timer interlocks, installer-provided previous-version identity, rollback activation and UI confirmation remain required before enabling it. No installed app/config/display was touched. Candidate `20260910b` predates this source change and must not be presented as containing it; the source-binding gate requires a fresh build.

### 2026-09-10: current-source candidate and packaging identity

- Fixed shortcut identity: frozen shortcuts target the running installed executable with `--show`; source shortcuts target canonical source using Python, never silently prefer a stale `dist` executable. Two synthetic shortcut tests passed without changing real shortcuts.
- Rechecked VPN invariance: 31 focused clock/schedule/runtime tests passed. Saved Dearborn Heights coordinates and direct Windows zone remain independent of IP location, proxy settings and process `TZ`. This is source proof, not a live VPN/installed-display trial.
- Recovered the completed `20260910a` build from its output; it used Python 3.13.5 while release metadata declared 3.14.7. It is a diagnostic artifact, not a release candidate to install. No old installed executable was used for this build.
- Build now records its Python implementation/version and a before-build source manifest, refusing source changes during compilation. Release packaging and archive verification compare that record with packaged source claims and CPython SBOM version, and verify every EXE's recorded hash/size. Missing evidence, stale source, runtime mismatch, replaced bytes and duplicate payload records fail closed. Added all nine Smart Comfort modules to source inputs and tzdata/tzlocal to SBOM components. These unsigned self-recorded checks are not signed provenance or full native dependency qualification.
- Installed managed Python 3.14.7 with `uv python install --no-bin`, then created isolated `audit/smart-comfort-runtime-3147` using frozen lock dependencies. Existing `.venv`, PATH shortcuts, user settings and installed app were not replaced. Source suite: **452 passed, 1 skipped** on both Python 3.13.5 (27.23s) and 3.14.7 (28.63s); focused binding/packaging **14 passed**; JavaScript **11 scenarios passed**.
- Fresh full build succeeded under `audit/smart-comfort-candidate-20260910b`. Its private unsigned `NightLight-private-candidate.zip` passed deterministic archive and build-binding checks, SHA-256 `6fae37de229e1837c1b3bb5f50564e934439fdbe07aacfa4b2ec9588b681bbc4`. Evidence is in that candidate's `evidence` folder. This artifact has not been installed, launched, signed, published or exposed on the website. Full SBOM native component qualification remains separate from the new CPython version check.
- Next integration work: generation-safe existing-user migration switch and owner adoption/rollback, then remaining UI/native verification and installer/download workflow. Read-only migration review currently keeps switching disabled; this candidate is not a completed user release.

The remaining task checkboxes stay open, including native reducer routing/bridge envelope, UI revision/reconciliation handling, persistent retry reconciliation, real readback/ownership qualification, bounded Smart trajectory and civil-time integration, explicit migration, onboarding/help, material/accessibility work, installer/site integration and all final evidence gates. Partial changes above do not complete those broader tasks.

Next: versioned intent/state and bounded trajectory integration, dated solar/personal planning, native Save confirmation proof, then the remaining onboarding/design/distribution work. Do not use the older concept as evidence for source or installed app.

Installation identity and archived old copies are documented in VERSION_CONSOLIDATION_2026-09-09.md. Do not reinstall the archived `dist` output. Build from the canonical current source only after applicable tests pass. Public signing/hosting/deployment, hardware qualifications, participant evaluation and fourteen actual nightly sessions remain distinct requirements, not claimed complete.

### 2026-09-10: retire legacy visual windows from normal QA

- User correctly identified the old Tk interface appearing during tests. The default suite retained ten legacy visual cases and one legacy native-control construction case; these now require explicit `NIGHT_LIGHT_TEST_LEGACY_UI=1`. Normal QA must use the premium interface; archived UI tests are not premium design evidence.
- Removed the unused ModernFlyout import and legacy flyout type annotation from TrayApp. Its actual constructor already selects PremiumFlyout.
- Focused premium/Smart/migration suite: 79 passed, 11 explicitly historical UI skips. These skips do not establish rendered premium coverage. Live process paths still point to the earlier installed feea8d build and its premium host; no new candidate has been installed or published, and no installed app or user settings were deleted.
- The preceding owned-file removal helper passed synthetic-only removal/retention tests in the full 513-pass run. Candidate f built successfully before this legacy-test correction, but was not packaged into a fresh setup or installed. It now predates current source and must not be represented as the latest build. Real upgrade/uninstall and newest premium desktop verification remain open.
